import React from "react";
import Box from '@mui/material/Box'

const Homepage = () => {
    return (
        <Box>

        </Box>
    );
};

export default Homepage;

// If signed in
    // Carousel of live streamers similar to stories section
    // Make Post
    // Feed
        // if not following anyone => Follow some accounts to populate your feed!

// If not signed in
    // Carousel of live streamers
    // Create an account button/message
    // Most Popular Vtubers
    // Most Popular Artists